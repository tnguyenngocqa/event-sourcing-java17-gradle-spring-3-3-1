server:
  port: 9003

spring:
  application:
    name: notificationservice

  kafka:
    consumer:
      group-id: ltfullstack

axon:
  axonserver:
    servers: axonserver:8124 # Connect to Axon Server on this address
